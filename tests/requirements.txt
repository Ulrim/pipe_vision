# AIVIS QA 하니스 실행 의존성 (tests/* 소유, P6 통합·검증).
#
# 아래 3개 소스를 먼저 설치한 가상환경에서 본 파일을 추가 설치한다:
#   pip install -e packages/shared-types/python          # aivis_types (공용 스키마)
#   pip install -r services/api/requirements.txt         # FastAPI/SQLAlchemy/TestClient(httpx) 등
#   pip install -r services/vision/requirements.txt      # OpenCV/numpy/onnxruntime 등
#
# 본 파일은 QA 전용 추가 도구만 명시한다(대부분 위에서 충족).
pytest>=8.0
httpx>=0.28        # FastAPI TestClient 백엔드(저장·연계율 검증, 지표4)
