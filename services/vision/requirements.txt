# AIVIS vision service (services/vision) — 추론 런타임 의존성
# 공용 스키마는 모노레포 패키지에서 설치:
#   pip install -e packages/shared-types/python   (제공 패키지: aivis_types)

opencv-python-headless>=4.9,<5
numpy>=1.26,<2.2
onnxruntime>=1.17,<2          # ONNX 표면 모델 배포(P7+). 미배포 시 고전 CV 폴백.
pydantic>=2.5,<3              # aivis_types 스키마(이미 의존). 명시 고정.

# 개발/테스트
pytest>=8.0
